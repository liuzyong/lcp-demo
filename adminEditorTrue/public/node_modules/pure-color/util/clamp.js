define('node_modules/pure-color/util/clamp', function(require, exports, module) {

  function clamp(val, min, max) {
    return Math.min(Math.max(val, min), max);
  }
  
  module.exports = clamp;

});
