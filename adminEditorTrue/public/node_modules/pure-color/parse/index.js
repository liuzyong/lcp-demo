define('node_modules/pure-color/parse/index', function(require, exports, module) {

  var hsl = require("node_modules/pure-color/parse/hsl");
  var hex = require("node_modules/pure-color/parse/hex");
  var rgb = require("node_modules/pure-color/parse/rgb");
  var hsl2rgb = require("node_modules/pure-color/convert/hsl2rgb");
  
  function hsl2rgbParse(color) {
    var h = hsl(color);
    var r = hsl2rgb(h);
  
    // handle alpha since hsl2rgb doesn't know (or care!) about it
    if(h.length === 4) {
      r.push(h[3]);
    }
  
    return r;
  }
  
  var space2parser = {
    "#" : hex,
    "hsl" : hsl2rgbParse,
    "rgb" : rgb
  };
  
  function parse(color) {
    for(var scheme in space2parser) {
      if(color.indexOf(scheme) === 0) {
        return space2parser[scheme](color);
      }
    }
  }
  
  parse.rgb = rgb;
  parse.hsl = hsl;
  parse.hex = hex;
  
  module.exports = parse;

});
