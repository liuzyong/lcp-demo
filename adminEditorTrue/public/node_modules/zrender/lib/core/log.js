define('node_modules/zrender/lib/core/log', function(require, exports, module) {

  var _config = require("node_modules/zrender/lib/config");
  
  var debugMode = _config.debugMode;
  
  var logError = function () {};
  
  if (debugMode === 1) {
    logError = console.error;
  }
  
  var _default = logError;
  module.exports = _default;

});
