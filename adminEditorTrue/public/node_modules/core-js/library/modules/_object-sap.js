define('node_modules/core-js/library/modules/_object-sap', function(require, exports, module) {

  // most Object methods by ES6 should accept primitives
  var $export = require('node_modules/core-js/library/modules/_export');
  var core = require('node_modules/core-js/library/modules/_core');
  var fails = require('node_modules/core-js/library/modules/_fails');
  module.exports = function (KEY, exec) {
    var fn = (core.Object || {})[KEY] || Object[KEY];
    var exp = {};
    exp[KEY] = exec(fn);
    $export($export.S + $export.F * fails(function () { fn(1); }), 'Object', exp);
  };
  

});
