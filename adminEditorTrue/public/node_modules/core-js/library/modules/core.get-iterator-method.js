define('node_modules/core-js/library/modules/core.get-iterator-method', function(require, exports, module) {

  var classof = require('node_modules/core-js/library/modules/_classof');
  var ITERATOR = require('node_modules/core-js/library/modules/_wks')('iterator');
  var Iterators = require('node_modules/core-js/library/modules/_iterators');
  module.exports = require('node_modules/core-js/library/modules/_core').getIteratorMethod = function (it) {
    if (it != undefined) return it[ITERATOR]
      || it['@@iterator']
      || Iterators[classof(it)];
  };
  

});
