define('node_modules/core-js/library/modules/_shared', function(require, exports, module) {

  var core = require('node_modules/core-js/library/modules/_core');
  var global = require('node_modules/core-js/library/modules/_global');
  var SHARED = '__core-js_shared__';
  var store = global[SHARED] || (global[SHARED] = {});
  
  (module.exports = function (key, value) {
    return store[key] || (store[key] = value !== undefined ? value : {});
  })('versions', []).push({
    version: core.version,
    mode: require('node_modules/core-js/library/modules/_library') ? 'pure' : 'global',
    copyright: '© 2019 Denis Pushkarev (zloirock.ru)'
  });
  

});
