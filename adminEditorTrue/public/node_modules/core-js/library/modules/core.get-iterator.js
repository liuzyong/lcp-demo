define('node_modules/core-js/library/modules/core.get-iterator', function(require, exports, module) {

  var anObject = require('node_modules/core-js/library/modules/_an-object');
  var get = require('node_modules/core-js/library/modules/core.get-iterator-method');
  module.exports = require('node_modules/core-js/library/modules/_core').getIterator = function (it) {
    var iterFn = get(it);
    if (typeof iterFn != 'function') throw TypeError(it + ' is not iterable!');
    return anObject(iterFn.call(it));
  };
  

});
