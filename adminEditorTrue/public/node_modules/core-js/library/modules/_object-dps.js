define('node_modules/core-js/library/modules/_object-dps', function(require, exports, module) {

  var dP = require('node_modules/core-js/library/modules/_object-dp');
  var anObject = require('node_modules/core-js/library/modules/_an-object');
  var getKeys = require('node_modules/core-js/library/modules/_object-keys');
  
  module.exports = require('node_modules/core-js/library/modules/_descriptors') ? Object.defineProperties : function defineProperties(O, Properties) {
    anObject(O);
    var keys = getKeys(Properties);
    var length = keys.length;
    var i = 0;
    var P;
    while (length > i) dP.f(O, P = keys[i++], Properties[P]);
    return O;
  };
  

});
