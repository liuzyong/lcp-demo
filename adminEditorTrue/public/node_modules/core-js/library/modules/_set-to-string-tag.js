define('node_modules/core-js/library/modules/_set-to-string-tag', function(require, exports, module) {

  var def = require('node_modules/core-js/library/modules/_object-dp').f;
  var has = require('node_modules/core-js/library/modules/_has');
  var TAG = require('node_modules/core-js/library/modules/_wks')('toStringTag');
  
  module.exports = function (it, tag, stat) {
    if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
  };
  

});
