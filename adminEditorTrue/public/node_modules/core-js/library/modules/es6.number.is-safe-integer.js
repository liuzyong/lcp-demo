define('node_modules/core-js/library/modules/es6.number.is-safe-integer', function(require, exports, module) {

  // 20.1.2.5 Number.isSafeInteger(number)
  var $export = require('node_modules/core-js/library/modules/_export');
  var isInteger = require('node_modules/core-js/library/modules/_is-integer');
  var abs = Math.abs;
  
  $export($export.S, 'Number', {
    isSafeInteger: function isSafeInteger(number) {
      return isInteger(number) && abs(number) <= 0x1fffffffffffff;
    }
  });
  

});
